# Firebase Configuration - Web SDK
# These are PUBLIC and safe to commit (Firebase client config)
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com

# ⚠️ CRITICAL SECURITY WARNING ⚠️
# The Azure OpenAI configuration below is INSECURE for production!
#
# Current Issue:
# - VITE_ prefix means these variables are bundled into your JavaScript
# - Anyone can open DevTools and extract your API key from the bundled code
# - Your API key will be visible in the production build
# - Attackers can steal and use your API quota
#
# REQUIRED FIX FOR PRODUCTION:
# 1. Create a backend API (serverless function on Vercel/Netlify/AWS Lambda)
# 2. Move these variables to backend-only environment variables (no VITE_ prefix)
# 3. Have your React app call YOUR backend, not Azure OpenAI directly
# 4. Implement authentication and rate limiting on your backend
#
# Example structure:
# - Frontend calls: POST /api/analyze-image
# - Backend receives request, validates user, calls Azure OpenAI
# - Backend returns result to frontend
#
# DO NOT deploy to production with client-side Azure OpenAI calls!

# Azure OpenAI Configuration (INSECURE - see warning above)
VITE_AZURE_OPENAI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
VITE_AZURE_OPENAI_API_KEY=your_api_key_here
VITE_AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
VITE_AZURE_OPENAI_API_VERSION=2024-04-01-preview

# Setup Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual credentials
# 3. NEVER commit .env.local to git (it's in .gitignore)
# 4. For production, implement the backend proxy solution described above
